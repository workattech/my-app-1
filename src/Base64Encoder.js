import React from 'react';
import NavBar from './NavBar';

function Base64Encoder() {
    return (
        <div>
            <NavBar />
            <h1>Base64Encoder</h1>
        </div>
    );
}

export default Base64Encoder;